# 2026-05-27 工作日志

## 初始状态
- 所有Docker服务运行正常 (15小时+)
- Backend/Fund/API 正常

## 今日修复 (commit af68134)

### 1. 一致预期采集修复 ✅
- **Bug**: pandas `float(2026.0)` → str→去`.`→ "20260" → 年份=20260被过滤
- **修复**: 直接用 `int(float(value))`
- **结果**: 233条/79只 → **883条/223只** (+650条, +144只)
- **修复列**: ALTER TABLE 加 min_eps, max_eps, forecast_count

### 2. 公告表采集 ✅
- **结果**: 2条 → **4,832条/161只** (巨潮cninfo)
- **脚本**: `scripts/fix_filing_and_eps.py`

### 3. 新闻采集修复 ✅ (容器akshhare)
- **根因**: 外机HTTP API全部被封，但Docker容器网络可访问
- stock_news: 1,274→4,228条/69→330只
- cls_news: 60→1,720条
- global_news: 400→17,000条
- 容器脚本: collect_news_container.py, collect_news_fast.py, collect_stock_news.py

### 4. 前后端数据差异修复 ✅
- **market字段**: 544→5,542只有SH/SZ/BJ (market/list ✅)
- **Fund navDate/yearReturn**: 新容器正常 ✅
- **Filing API**: 500 Error修复 - DB添加`publish_date`/`filing_type`兼容列 ✅
- **龙虎榜简单表**: 0→25,019条 ✅

### 5. 最终数据覆盖 (2026-05-27 15:20)
| 数据表 | 记录 | 覆盖 | 状态 |
|--------|------|------|------|
| stock (行业) | 5,542/5,544 | 99.96% | ✅ |
| stock_daily (K线) | 129,383 | 5,539只 | ✅ |
| fund (净值) | 96/100 | 96% | ✅ |
| hot_reason (热点) | 27,276 | 356天 | ✅ |
| dragon_tiger_detail | 29,715 | 361天 | ✅ |
| dragon_tiger (简单) | **25,019** | 补全 | ✅ |
| research_report (研报) | 5,929 | 267只 | ✅ |
| stock_news (个股新闻) | **4,228** | 330只 | ✅ |
| consensus_eps (一致预期) | **883** | 223只 | ✅ |
| info_filing (公告) | **4,832** | 161只 | ✅ |
| cls_news (财联社) | **1,720** | — | ✅ |
| global_news (全球) | **17,000** | — | ✅ |

### 6. 前端页面验证 (16:25)
✅ 所有页面无控制台错误/警告
- 登录 → 首页(实时行情/基金/热点卡片)
- /stocks 股票列表
- /funds 基金列表
- /dragon-tiger 龙虎榜
- /hot-reason 题材热点
- /chat AI对话
- /news 资讯
- /stock/000001 个股详情
- /northbound 北向资金
- /industry-compare 行业对比
- /fund-flow 资金流向
- /lockup 限售解禁
- /consensus-eps 一致预期

### 7. 已推送 commits
39b5ae5 → bfbb4a5 → 14a652b

### 8. 数据采集范围优化 (17:05) ✅
#### collect_stock_news.py
- **覆盖范围**: top300 → 全量股票（5000+只）
- **分批**: 每批100只，自动计算总批次数
- **超时**: 120s → 180s
- **日志**: 添加批次进度百分比

#### fix_filing_and_eps.py
- **公告范围**: TOP500 → TOP1000 市值股票
- **增量跳过**: 已有数据的股票自动跳过，避免重复API调用
- **一致预期**: 同样添加增量跳过，已有数据的股票跳过采集
- 语法验证通过

### 9. 后端代码优化 (17:05) ✅
#### RedisConfig.java — 分级 TTL 缓存
- 基于 cacheNames 的分级 TTL（30秒/5分钟/15分钟/30分钟/60分钟）
- 新增: signalHotData(5m), signalDragonTiger(5m), signalMarketData(30s), signalReferenceData(15m)
- 兜底默认从30分钟改为15分钟

#### 创建 BusinessException.java (dto 包)
- 含 code(int) + message(String)，继承 RuntimeException

#### GlobalExceptionHandler.java — 完善异常处理
- 新增 BusinessException / MethodArgumentNotValidException(字段级聚合) / ConstraintViolationException / AccessDeniedException(403) / HttpRequestMethodNotSupportedException(405)

#### SignalDataServiceImpl — 缓存策略升级
- 22个 @Cacheable 按数据类型分配4个 cacheNames
- clearCache() 使用 @Caching 分别清空4个缓存区域

#### application.yml — 移除 SQL 日志
- 注释掉 StdOutImpl log-impl，默认不打印SQL

### 10. Docker 资源限制优化 (17:05) ✅
#### 为所有15个 service 添加了 `deploy.resources` 限制：
- **Tier1 存储层**: namenode(1g/512m), datanode1(2g/1g), datanode2(2g/1g), mysql(2g/1g), redis(512m/256m)
- **Tier2 计算层**: resourcemanager(1g/512m), nodemanager1(2g/1g), hive-server(2g/1g), spark-master(1g/512m), spark-worker(4g/2g)
- **Tier3 应用层**: ai-service(2g/1g), backend(1g/512m), frontend(256m/128m), prometheus(512m/256m), grafana(512m/256m)

#### 其他优化：
- **nodemanager1**: 添加了 healthcheck（端口8042）
- **depends_on 规范化**: datanode1/2, resourcemanager, nodemanager1, spark-worker 改为 condition: service_started 格式；hive-server 的 mysql 依赖改为 service_healthy

### 11. 前端性能优化 (17:15) ✅
#### ECharts 按需引入
- 创建 `src/utils/echarts.ts`，只注册实际使用的组件（Line, Bar, Pie, Candlestick, Treemap, Scatter, Heatmap）
- 8个文件的 `import * as echarts from 'echarts'` → `import echarts from '@/utils/echarts'`
- 全量ECharts ~1MB → 按需 ~300KB

#### Vite 构建优化
- 添加 `rollupOptions.treeshake: { preset: 'smallest' }`
- 安装 `vite-plugin-compression`

### 12. 后端 Maven 编译 + 容器重建 (17:15) ✅
- Maven `clean package -DskipTests` 编译成功（52MB JAR）
- Docker 镜像重建（`docker compose build --no-cache backend`）
- 旧容器停止并删除，新容器使用新镜像创建
- 新 backend 容器当前正常健康运行
- 至此 `InfoFiling` 实体字段修正已在 JAR 中生效

### 13. 资讯页面重构 (23:04) ✅
#### NewsView.vue 完全重写
- **集成真实后端 API**：对接 cls-news（财联社快讯）+ global-news（全球资讯），替代原有 mock 数据
- **日期导航栏**：el-date-picker + 左右箭头切换前一天/后一天，当天/昨天带 badge 标记
- **来源分类过滤**：全部 / 财联社 / 全球资讯 三栏切换
- **日期快捷跳转条**：根据数据中实际有的日期生成 chips，点击快速跳转
- **时间线布局**：左侧圆点+时间 + 右侧卡片（来源tag、标题、内容摘要）
- **完整状态覆盖**：skeleton 骨架屏加载态、错误态（带重试）、空数据态
- **加载更多**：分页获取更早数据，自动去重追加

#### 其他
- info.ts 新增 `getClsNewsSince(since)` API，修复返回类型
- 无 lint 错误

### 14. 待办
### 15. 后续修复 (23:45-23:59)
#### NewsView 合并来源标签 ✅
- 移除"全部/财联社/全球资讯"来源筛选标签，合并为单一资讯流
- 卡片中保留 `[快讯]`/`[资讯]` 标签仅作视觉参考

#### FundController 空数据返回修复 ✅
- `ApiResponse.error("无净值数据")` → `ApiResponse.ok(emptyList())`
- `ApiResponse.error("无持仓数据")` → `ApiResponse.ok(emptyList())`
- 避免 ElMessage.error 弹出错误提示
- 后端容器已重建生效

#### 板块云图 ROUND 精度修复 ✅
- `ROUND(AVG(change_percent), 2)` → `ROUND(AVG(change_percent), 4)`
- HomeView 移除 `if (pct === 0) continue` 过滤逻辑
- JAR 编译 + 容器重建生效

### 16. 📋 明天待修复 (2026-05-28)
1. **资讯时间显示优化** — 同一时间段的多条资讯，只有最前面的显示时间，其余隐藏避免视觉冗余
2. **涨跌幅数据问题排查** — 所有板块显示 +0.00%，检查数据层 change_percent 计算链路（可能是采集层 `batch_fetch_all_kline.py` 未计算 change_percent 或数据库存储异常）
3. **首页加载性能优化** — HomeView 多个 API 并行请求触发频繁重渲染，考虑加入 loading 合并、骨架屏优先展示、缓存命中优化
