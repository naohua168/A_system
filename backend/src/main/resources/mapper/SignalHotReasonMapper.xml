<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stock.mapper.SignalHotReasonMapper">

    <!-- 修复: 添加 LIMIT #{limit} OFFSET #{offset} 防止全表扫描 -->
    <select id="selectByDateRange" resultType="com.stock.entity.SignalHotReason">
        SELECT * FROM signal_hot_reason
        WHERE trade_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY change_pct DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 修复: 新增 countByDateRange 实现，匹配 Java 接口声明 -->
    <select id="countByDateRange" resultType="long">
        SELECT COUNT(*) FROM signal_hot_reason
        WHERE trade_date BETWEEN #{startDate} AND #{endDate}
    </select>

</mapper>
