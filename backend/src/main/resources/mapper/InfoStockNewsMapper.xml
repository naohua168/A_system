<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stock.mapper.InfoStockNewsMapper">

    <select id="selectByStock" resultType="com.stock.entity.InfoStockNews">
        SELECT * FROM info_stock_news
        WHERE stock_code = #{code}
        ORDER BY publish_time DESC
        LIMIT #{limit}
    </select>

    <select id="selectByStockSince" resultType="com.stock.entity.InfoStockNews">
        SELECT * FROM info_stock_news
        WHERE stock_code = #{code}
          AND publish_time >= #{since}
        ORDER BY publish_time DESC
    </select>
</mapper>
