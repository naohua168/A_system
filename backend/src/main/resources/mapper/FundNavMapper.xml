<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stock.mapper.FundNavMapper">

    <!-- 净值走势查询 -->
    <select id="selectNavTrend" resultType="com.stock.entity.FundNav">
        SELECT *
        FROM fund_nav
        WHERE fund_code = #{fundCode}
          AND nav_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY nav_date ASC
    </select>

    <!-- 查询最新净值 -->
    <select id="selectLatestNav" resultType="com.stock.entity.FundNav">
        SELECT *
        FROM fund_nav
        WHERE fund_code = #{fundCode}
        ORDER BY nav_date DESC
        LIMIT 1
    </select>

    <!-- 查询某时间点附近净值 -->
    <select id="selectNavByDate" resultType="com.stock.entity.FundNav">
        SELECT *
        FROM fund_nav
        WHERE fund_code = #{fundCode}
          AND nav_date &lt;= #{date}
        ORDER BY nav_date DESC
        LIMIT 1
    </select>

</mapper>
